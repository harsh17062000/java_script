import { createSlice } from "@reduxjs/toolkit"

function App() {
  const [count, setCount] = useState(0)

  return (
    <>
      {/* SLice                            / store->glodbal
        ->name
        ->initial
        ->reducer:
        {

        } */}

        


    </>
  )
}

export default App
